server:
  port: 8082
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true   # 요청/응답 모두 UTF-8 강제 적용

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/delivery_v0?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: knu74501@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # 엔티티와 DB 스키마 일치 여부만 검증
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQLDialect

  redis:
    host: localhost
    port: 6379
    password: ""   # 비밀번호 없으면 빈 문자열로 처리
